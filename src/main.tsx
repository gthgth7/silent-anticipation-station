
// This file exists to support the build process
// The actual content is served from the static HTML in public/token/create-coin.html
console.log("Application is using static HTML from public/token directory");

// No need to mount React app since we're using the static HTML directly
